defn funA a = {a + 1 + 2}

defn funB a b = {(a + -1) * .2 + (b - 3.4e-2) / 5.6}

defn funC a b c = {~((a % b) & c | a)}

defn funD a b c d = {a && !b || (c != d)}

defn funE a b = {((a < b) != (a >= b)) == ((a <= b) != (a > b))}

defn funF a b = {a << b >> b}

defn funG a b = {funF (funA a) (b + 1)}

defn funH = {-(-1 - -1)}

defn funI = {funA 1 - 2}

defn funJ a b = {a_b}

defn funK = {[1, 2, 3,]}

defn funL = {"A said \"Hello\" to B"}

defn funM = {['a', 'A',] ++ ['\n',]}

defn funN = {[True, False,]}

defn hello = {"hello world!"}

defn helloo = {"hello world!\\\""}  # \\ and \"

defn trueOrFalse = {True || False}

defn get a b = {a_b}

defn makeList a b c = {[a, b, c,]}