defn fun_a a = {a + 1 + 2}

defn fun_b a b = {(a + -1) * .2 + (b - 3.4e-2) / 5.6}

defn fun_c a b c = {~((a % b) & c | a)}

defn fun_d a b c d = {a && !b || c ^ d}

defn fun_e a b = {((a < b) != (a >= b)) == ((a <= b) != (a > b))}

defn fun_f a b = {a << b >> b}

defn fun_g a b  = {fun_f fun_a a (b + 1)}

defn fun_h = {-(-1 - -1)}

defn fun_i = {fun_a 1 - 2}