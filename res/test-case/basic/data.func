data Color = {Red, Green, Blue}

data Shape = {Circle Int, Rectangle Int Int}

data Pair a = {MkPair a a}  # new

defn area shape = {
    case shape of {
        Circle r -> {r * r * 3}
        Rectangle w h -> {w * h}
    }
}